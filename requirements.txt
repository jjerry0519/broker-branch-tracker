# Core pipeline (ingest.*) + tests. Plain HTTP only -- no browser, no CAPTCHA.
httpx==0.27.2
pyarrow==18.1.0
duckdb==1.1.3
pytest==8.3.3

# -r requirements-fallback.txt  # only for fallback.* (official-source scrapers)
